//https://open.kattis.com/problems/halfacookie
#include <iostream>
#include <fstream>      // std::ifstream

using namespace std;

int main(){

  long double r, x, y;
  while (  ){
      cin >> r >> x >> y;
  }

  cout.precision(10);
  cout << r << x << y << endl;
  return 0;
}
